# ASP.NET Core tutorials(ASP.NET Core 指南)
The following step-by-step guides for developing ASP.NET Core applications are available:

下面的分部指南可用于开发ASP.NET Core应用程序:

### Building web applications(构建web应用程序)
Razor Pages is the recommended approach to create a new Web UI app with ASP.NET Core 2.0.

Razor Pages是ASP.NET Core2.0创建web UI应用程序的推荐方式.
- Introduction to Razor Pages in ASP.NET Core(ASP.NET Core Razor Pages介绍)
- Create a Razor Pages web app with ASP.NET Core(ASP.NET Core创建Razor Pages web应用)
    - Razor Pages on Windows(在Windows上创建Razor Pages)
    - Razor Pages on Mac(在Mac上创建Razor Pages)
    - Razor Pages with VS Code(使用VS Code创建Razor Pages)
- Create an ASP.NET Core MVC web app(创建ASP.NET Core MVC web应用)
    - Web app with Visual Studio for Windows(在Windows上使用Visual Studio创建web应用)
    - Web app with Visual Studio for Mac(在Mac上使用Visual Studio创建web应用)
    - Web app with Visual Studio Code on Mac or Linux(在Mac或Linux上使用Visual Studio Code创建web应用)
- Getting started with ASP.NET Core and Entity Framework Core using Visual Studio(使用Visual Studio,开始ASP.NET Core以及Entity Framework Core框架开发)
- Authoring Tag Helpers(创建标签助手方法)
- Creating a simple view component(创建简单的视图组件)
- Developing ASP.NET Core applications using dotnet watch(使用dotnet watch开发ASP.NET Core应用程序)

### Building web APIs(构建web APIs)
- Create a Web API with ASP.NET Core(使用ASP.NET Core创建web API)
    - Web API with Visual Studio for Windows(在Windows上使用Visual Studio创建Web API)
    - Web API with Visual Studio for Mac(在Mac上使用Visual Studio创建Web API)
    - Web API with Visual Studio Code(使用Visual Studio Code创建Web API)
- ASP.NET Core Web API Help Pages using Swagger(使用Swagger创建ASP.NET Core Web API帮助页面)
- Creating backend web services for native mobile applications(为本地移动应用创建后端web服务)

### Working with data(使用数据)
- Getting started with ASP.NET Core and Entity Framework Core using Visual Studio
- ASP.NET Core with EF Core - new database(新数据)
- ASP.NET Core with EF Core - existing database(已存在数据库)

### Authentication and authorization(认证和授权)
- Enabling authentication using Facebook, Google and other external providers(使用Facebook,Google以及其他第三方提供程序启用身份验证)
- Account Confirmation and Password Recovery(账号确认与密码重置)
- Two-factor authentication with SMS(使用SMS启用双重认证-Two-factor)

### Client-side development(客户端开发)
- Using Gulp(使用Gulp)
- Using Grunt(使用Grunt)
- Manage client-side packages with Bower(使用Bower管理客户端packages)
- Building beautiful, responsive sites with Bootstrap(使用Bootstrap构建漂亮,响应式网站)

### Testing(测试)
- Unit Testing in .NET Core using dotnet test

### Publishing and deployment
- Deploy an ASP.NET Core web app to Azure using Visual Studio(使用Visual Studio部署ASP.NET Core web应用至Azure云)
- Deploy an ASP.NET Core web app to Azure using the command line(使用命令行部署ASP.NET Core web应用至Azure云)
- Publishing to an Azure Web App with Continuous Deployment(通过持续部署,发布Azure云Web应用)
- Deploy an ASP.NET container to a remote Docker host(将ASP.NET容器部署到远程Docker主机)
- ASP.NET Core on Nano Server(在Nano服务商部署ASP.NET Core)
- ASP.NET Core and Azure Service Fabric(ASP.NET Core 和 Azure Service Fabric)

### How to download a sample(如何下载例子)
- Download the ASP.NET repository zip file(下载ASP.NET仓库zip文件).
- Unzip the Docs-master.zip file(解压Docs-master.zip文件).
- Use the URL in the sample link to help you navigate to the sample directory(使用示例链接中的 URL 帮助你导航到示例目录).